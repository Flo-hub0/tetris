#ifndef menu_basis
#define menu_basis

#include <gtk/gtk.h>

static void print_hello(GtkWidget *widget, gpointer data);
static void launch_playing_window(GtkWidget *widget, gpointer data);
static void activate(GtkApplication *app, gpointer user_data);

#endif

